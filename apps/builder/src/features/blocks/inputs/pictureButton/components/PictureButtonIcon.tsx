import { ImageIcon } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const PictureButtonIcon = (props: IconProps) => (
  <ImageIcon color="orange.500" {...props} />
)
